import { FormatAMPMPipePipe } from './format-ampmpipe.pipe';

describe('FormatAMPMPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAMPMPipePipe();
    expect(pipe).toBeTruthy();
  });
});
